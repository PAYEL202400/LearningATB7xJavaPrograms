package ex_14072024;

public class lab068 {
    public static void main(String[] args) {
        String name = "Pramod";// Assignment operator
        //String name2 = new String ("Pramod");// new operator

        //bpth of them actually saves the name, name2 in different ways JVM
        //How they stores the values in JVM?

        //SCP AND HEAP
        //when we use assignment operator string is stored in SCP(string constant pool)
        //when we use new operator the string is stored in heaps

        //String are immutable-- that cannot be changed.
    }
}
